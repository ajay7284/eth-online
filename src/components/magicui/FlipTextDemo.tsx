import FlipText from "../lib/Flipword";

export async function FlipTextDemo() {
  return (
    <FlipText
      className="text-xl font-bold tracking-[-0.1em] text-black    ml-[10px]  dark:text-white"
      word="Using the place value chart we identify the place for each digit in the given number and write the numbers in words.
       "
    />
  );
}
